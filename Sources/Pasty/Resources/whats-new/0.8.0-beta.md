# Pasty v0.8.0-beta — ノッチが「もう一段」便利に。

「マウスでもサクサク、これから iCloud 同期。」

## ✨ 新機能

### 1. マウスホイールでカルーセル横スクロール
ストリップ／ノッチホバーの上でマウスホイールを縦に回すと、カルーセルが横にスクロールします。
トラックパッドはもちろん、Magic Mouse やクラシックなホイールマウスでも快適に切り替えられるようになりました。
- 既に横スワイプしている時はそのままパススルー
- スクロール開始時にホバープレビューを自動 dismiss して被りを防止
- 設定 → サーフェス → 「ホイールで横スクロール」でいつでも OFF にできます

### 2. C1 iCloud 同期 — 設計フェーズ完了 🛠️
複数の Mac 間でクリップ履歴を同期する大型機能の **設計フェーズが完了** しました。
今リリースでは UI 上の設定項目は「実験的・未実装」として無効化されており、実装は v0.9 で進めます。

設計ドキュメント (3 本) は `.ai/decisions/c1-icloud-sync-*.md` に格納:
- **architecture**: CloudKit Private DB ベース、Lamport クロック、LWW + content-hash dedupe
- **security**: STRIDE スレッドモデル、CryptoKit AES-GCM-256 / HKDF-SHA256 / ECDH P-256 / Ed25519、E2E 必須、CVSS タグ付き脆弱性 4 件
- **schema**: `sync_journal` テーブル DDL、各テーブルへの soft delete + `entity_uuid` + `origin_device_id` カラム追加

ReviewAgent によるクロスドキュメント整合性チェックも完了し (Score 88/100)、**Go 判定** で実装フェーズへ進みます。

### 3. DB スキーマ足場 (前方互換)
v0.9 で本実装される iCloud 同期に備えて、**今リリースで DB スキーマだけ先に拡張** しました。
既存ユーザーの DB は起動時に自動でマイグレーションされます。

具体的に増えるもの:
- `sync_journal` テーブル (Lamport / device_id / encrypted_payload / nonce / schema_version)
- `clips` / `pinboards` / `pinboard_items` に `deleted_at` / `updated_at` / `origin_device_id` / `entity_uuid` カラム
- 新規クリップ挿入時、自動で `origin_device_id` と `entity_uuid` がスタンプされます

CloudSyncEngine 自体は phase 2 で稼働します。今は no-op スタブ — 既存の動作には一切影響しません。

## 🛠 内部改善

- WhatsNewPresenter のリリースノート表示パスを 0.8.0-beta に対応
- ClipStore マイグレーションが v4 → v5 系列に拡張 (`v5.sync_journal` / `v5.soft_delete_columns` / `v5.entity_uuid_backfill`)
- 設定画面プライバシータブに「iCloud 同期」セクションを追加 (現状は disabled トグル)
- Settings の「サーフェス」ヒント文を更新

## 🔧 既知の制約 / 注意

- C1 iCloud 同期の **データ送信は一切行いません**。phase 2 までは完全ローカル動作です。
- 設定の「iCloud 同期を有効化」トグルは UI 上のプレビューであり、現状は無効化されています。
- マウスホイール横スクロールはデフォルト ON です。気に入らなければ設定からすぐ OFF にできます。

---

リリース詳細: https://github.com/IvyGain/Pasty/releases/tag/v0.8.0-beta
